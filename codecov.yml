coverage:
  status:
    project:
      default:
        target: 90%
    patch:
      default:
        target: 90%
ignore:
    - src/cpu/opcodes/operand.rs
    - "xtask/**"            # build & deploy tooling, not part of the emulator
