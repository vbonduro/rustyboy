[package]
name = "rustyboy-web-client"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustyboy-core = { path = "../../../core" }
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
